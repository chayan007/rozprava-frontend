let groupConfig;

groupConfig = {
    api: {
        createGroup:{
            endpoint: "profile/group/",  
        },
        searchProfile:{
            endpoint: "profile/search/{0}",
        }   
    }
}

export {groupConfig}